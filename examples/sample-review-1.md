# Sample Review 1

**PR reviewed:** https://github.com/cli/cli/pull/100
**Command run:**
```
claude-review --pr https://github.com/cli/cli/pull/100
```

**PR stats:** +88 lines, 1 file changed (new `gh.1.md` man page)
**Heuristic result:** confidence=High, risk=MEDIUM (no test file changes)
**Claude verdict:** LOW risk · REQUEST_CHANGES

---

<!-- claude-review -->
> **Claude PR Review** — generated by [claude-review](https://github.com/claude-builders-bounty/claude-builders-bounty/issues/4)

## Summary
This PR adds an initial man page (`gh.1.md`) for the `gh` CLI tool using pandoc-style markdown-to-manpage format. It covers basic synopsis, global options, a partial list of commands, and standard manpage sections. The content is a clear work-in-progress draft with several TODO placeholders and minor issues.

## Risks
- The BUGS section links to `https://github.com/github/gh-cli/issues`, which appears to be an incorrect repository URL (the PR is against `cli/cli`). Users following this link would land on a 404 or wrong project.
- Typo in DESCRIPTION: "Do thinks with GitHub" should be "Do things with GitHub."
- Multiple TODO placeholders left in the document (`TODO add rest of commands / more examples`, `TODO` under ENVIRONMENT), which could ship incomplete if merged without follow-up.

## Suggestions
- Fix the typo: "Do **thinks**" → "Do **things**".
- Update the BUGS URL to match the actual repository (e.g., `https://github.com/cli/cli/issues`).
- The `pr create` section lists three separate definition terms (`pr create`, `pr create --draft`, `pr create --title ...`) but only a single description block, and that block uses `:` instead of `:   ` (missing indentation), which is inconsistent with the other definition list entries. Consolidate into one term with examples in the body, or fix the formatting.
- Consider adding a note in the PR description or a tracking issue for the TODOs so they don't get lost.
- The version is hardcoded as `0.0.0` — consider whether this should be templated or updated to reflect an actual version.

## Verdict
**Confidence:** High

**Risk level:** LOW — This is a documentation-only addition with no code changes; the main risks are an incorrect URL and a typo, both easily fixed.

**Recommendation:** REQUEST_CHANGES

---
*Powered by Claude Sonnet · [claude-review](https://github.com/claude-builders-bounty/claude-builders-bounty/issues/4)*
